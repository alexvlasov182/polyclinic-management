<div class="row">
  <div class="col-md-12">
    <nav class="navbar navbar-expand-lg navbar-light bg-white px-4 custom-style-nav">
      <a class="navbar-brand color-logo" href="<%= root_path %>"> Polyclinic Management</a>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to('Logout', destroy_user_session_path, method: :delete, class: 'nav-link') %>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</div>
